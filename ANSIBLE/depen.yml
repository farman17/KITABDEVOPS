- name: ANSIBLE-COLONYLABS@2022
  hosts: testing2
  remote_user: testing2
  become: yes
  become_method: sudo
  roles:
    - update
    - dependen
    #- nginx
    #- docker
    #- corosync-cluster
    #- corosync-client
    #- node_exporter
    #- grafana_prom
    #- harbor
    - k3s-kubernetes
    # - awx-ansible
    #- minio
    # - webmin
    #- jenkins
    #- redis
    #- redis-replica
    #- SUMMARY
