- name: ANSIBLE-COLONYLABS@2022
  hosts: testing
  remote_user: testing
  become: yes
  become_method: sudo
  roles:
    - update
    - dependen
    #- docker
    #- node_exporter
    #- nginx
    #- grafana_prom
    #- harbor
    #- k3s-kubernetes
    # - awx-ansible
    - minio
    # - webmin
    # - jenkins
    - SUMMARY
