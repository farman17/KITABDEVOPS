- name: ANSIBLE-COLONYLABS@2022
  hosts: slavedua
  remote_user: testing3
  become: yes
  become_method: sudo
  vars:
    ansible_become_pass: "{{ vault_sudo_password }}"
  roles:
    - update
    - dependen
    #- nginx
    #- docker
    #- corosync-cluster
    #- corosync-client
    - node_exporter
    - grafana_prom
    #- harbor
    #- k3s-kubernetes
    #- awx-ansible
    #- minio
    # - webmin
    #- jenkins
    #- redis
    #- redis-replica
    #- SUMMARY
